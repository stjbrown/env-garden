# Never commit real profiles (they may contain secrets); ship only *.example.
.env
.env.*
!*.env.*.example
!*.env.example

# 1Password / backup artifacts
*.eg-bak-*

# Build output
/dist/
/eg
*.out

# Local-only launch material (tweet drafts) — keep out of the repo
/docs/ANNOUNCEMENT.md
